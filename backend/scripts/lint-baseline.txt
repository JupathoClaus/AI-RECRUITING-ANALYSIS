# Lint Baseline - Pre-existing violations
# Format: relative_path:rule_id
# Full lint requires --max-old-space-size=4096 due to project size
# This baseline tracks known violations; new ones must not be added
