"use client"

import { CandidateProfile } from "@/components/candidates/candidate-profile"
import { AppLayout } from "@/components/layout/app-layout"

export default function CandidateProfilePage() { return <AppLayout title="Candidate profile" description="Candidate information remains distinct from each application."><CandidateProfile /></AppLayout> }
